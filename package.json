{
  "name": "sipm-ui-tooling",
  "private": true,
  "type": "module",
  "scripts": {
    "audit": "npm run audit:python && npm run audit:ui",
    "audit:python": "uvx pip-audit==2.10.1 -r requirements.txt",
    "audit:ui": "npm audit --audit-level=high",
    "lint": "npm run lint:python && npm run lint:ui",
    "lint:python": "uvx ruff==0.15.21 check src/main/backend src/main/test scripts deployment/mock-packages/treasury_analytics/src",
    "lint:ui": "eslint src/main/ui/js src/main/ui/test playwright.config.js",
    "test": "npm run test:backend && npm run test:ui",
    "test:coverage": "npm run test:backend:coverage && npm run test:ui:coverage",
    "test:backend": "python -m pytest -m \"not integration\"",
    "test:backend:coverage": "python -m pytest -m \"not integration\" --cov --cov-report=term-missing --cov-report=xml",
    "test:backend:integration": "python -m pytest -m integration",
    "test:ui": "vitest run",
    "test:ui:coverage": "vitest run --coverage",
    "test:ui:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@vitest/coverage-v8": "^4.1.8",
    "eslint": "^10.8.0",
    "globals": "^17.9.0",
    "jsdom": "^26.0.0",
    "vitest": "^4.1.8"
  }
}
