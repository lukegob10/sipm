interface:
  display_name: "SIPM Agent"
  short_description: "Submit approval-gated SIPM agent change requests."
  default_prompt: "Use the SIPM Agent skill to read scoped SIPM work context and submit approval-gated project manager changes."
